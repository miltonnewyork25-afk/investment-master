# 产业链全景分析器 v3.0 (Industry Chain Mapper)

> **定位**: 把一条产业链做成"谁在哪个周期、谁拿到利润、谁先被去杠杆、谁被市场重新定价"的完整分析。跨行业可迁移,不只服务 AI 产业链。
> **v3.0 硬产出清单(7 项,缺任一不得提交)**:
> 1. 主报告 `reports/{CHAIN_NAME}/Chain_Framework_{DATE}.md`(70-170K 字符)
> 2. **执行摘要** — 主报告开头 3 页,固定 5 问结构
> 3. **三榜决策清单** — 结构性核心池 / 当前研究优先池 / 交易回调池,禁止合并为单一 Top 20
> 4. **12 列母表 + 4 列可审计字段 = 16 列活数据库** `data/module_master_table_{DATE}.md`
> 5. **每模块一张证据卡**(3 支持 + 2 削弱 + 1 关键图 + 1 失效触发 + 1 最优表达 + 1 最危险错法)
> 6. **三分系统评分表** `data/three_score_table_{DATE}.md`(结构质量分/当前定价分/财务韧性分)
> 7. **变化日志** `data/changelog_{DATE}.md`(新增证据 / 删除证据 / 排序变化 / 失效条目)
> **v3.0 升级核心**: 把 v2.0 的"结构/母表/四修正"进一步固化为"执行摘要+三榜+证据卡+三分+变化日志"的五项硬交付,让下一次运行默认达到"顶级研究稿"水准,不退回单一 Top 20 / 单一估值尺子 / 一次性观点表。
> **v1.0 验证**: AI 产业链(2026-04-20,74K)。**v2.0 验证**: AI 产业地图 v6(2026-04-20,四层系统+12 列母表)。**v3.0 验证**: AI 产业地图 final(2026-04-20,170K,执行摘要+三榜+13 张证据卡+三分+变化日志齐全)。

---

## 一、这个 skill 解决什么问题

普通产业链报告最常见的三个问题:

1. **只列"谁在链上",不回答"谁拿到钱"**——读完还是不知道哪家值得买。
2. **把节点相近的公司混成一组**——例如把所有"AI 电力"放一篮子、把所有"AI 测试"放一篮子,忽略它们在信用 / 商誉 / FCFE / 客户集中度上的巨大差异。
3. **风险章节用"宏观不确定性"等通用词搪塞**——不写清楚"下一轮回调时谁先被重定价"。

v2.0 在这三条之上再加一条:

4. **写成"静态观点表",而不是"可更新工作底板"**——每次都要重写散文,无法跨板块迁移。v2.0 用 **12 列母表**解决这一条。

---

## 二、触发条件

| 场景 | 触发 |
|------|------|
| **手动** | `/chain-map {CHAIN_NAME}`(例 `/chain-map AI` / `/chain-map EV` / `/chain-map Obesity`) |
| **跨报告复盘** | 完成 ≥5 家同行业独立公司报告后,自动建议触发,把个股视角升级到链条视角 |
| **行业切换期** | 当一个行业出现"主题转折 / 资金轮动 / 信用弱点暴露"三者任一时,触发链条层面重绘 |

**不触发场景**:
- 单一公司分析(用 Tier 1-3 公司分析 skill)
- 子链分析(用各自专用 skill,例 `moat-evaluator`、`valuation-builder`)

---

## 三、v2.0 顶层架构:四层系统图(取代 v1.0 的"三层结构")

v2.0 的核心升级是**不再把产业链画成"谁在链上"的横向清单**,而是分成四层系统:

| 层 | 回答什么问题 | 内容 |
|---|---|---|
| **结构层** | 系统里到底有什么 | 预算源 / 物理主链 / 部署扩散链 / 回流商业化层 的**稀缺事物**(不是板块名) |
| **约束层** | 什么在控制这张图 | 预算约束 / 物理供给约束 / 部署约束 / 利润留存约束 / 区域角色约束 |
| **时钟层** | 谁先兑现谁后兑现 | 预算 / 设计与资格导入 / 制造扩产 / 交付 / 上电与热管理 / 商业化回流 / 现金兑现 / 市场认知 八个时钟 |
| **定价层** | 现在该拿哪一块 | 结构性溢价 vs 主题溢价 / 利润中心 vs 放大量 / 供给弹性 / 财务弹性 / 核心或交易 / 四分法 |

**串联关系(不是并列标签)**:

```
结构层("里面有什么") → 约束层("什么卡住它") → 时钟层("什么时候兑现") → 定价层("现在该拿哪一块")
```

**丢一层就退化**:
- 只有结构层 = 名单
- 结构 + 约束 = 静态评分
- 结构 + 约束 + 时钟 = 缺价格纪律
- 四层齐全 = 真正能指导研究、排序和仓位的运行地图

---

## 四、v2.0 的四个核心修正(指针,贯穿全报告)

写每一章之前,必须先用这四条校准方向,否则会悄悄退回旧的清单式产业链:

### 修正 1:从"模块图"改成"事物图"

不再只写板块名(GPU / HBM / 封装 / 设备 / 服务器 / 光模块),而是写它们代表的**真正稀缺事物**:预算 / 算力 / 内存带宽能力 / 先进制造与封装整合能力 / 上电能力 / 配电与保护能力 / 热管理能力 / 板级高速互连能力 / 已知良品筛选能力。

### 修正 2:从"受益关系"改成"作用关系"

不再只问"谁受益于 X",而是追问:谁**转化**预算、谁**组织供给**、谁**负责部署**、谁**形成约束**、谁**形成回流**。

### 修正 3:从"静态层级"改成"动态多钟"

不再把所有受益股当同一轮行情同时兑现,而是用八个时钟分别定位。**经营兑现时钟**和**市场定价时钟**必须分开看——"基本面没错,但价格已经走太远"只有两个时钟分开才能看见。

### 修正 4:从"谁重要"改成"谁值得研究 / 值得拿 / 只适合交易"

最接近投资动作的一步。**重要 ≠ 最适合买**。四个动作层:
- 结构核心 + 高质量利润池 → 核心跟踪 / 回调优先看
- 长周期基础设施承接层 → 中期研究 / 优先深挖
- 重要但利润浅的配套层 → 选择性跟踪 / 研究观察
- 高 beta 高拥挤交易层 → 交易观察 / 不做默认核心池

---

## 五、v2.0 硬产出:12 列母表

**这是 v2.0 最重要的落地物**。母表不是静态观点表,而是可反复更新的工作底板。跨板块切换时,只需换数据,不换结构。

### 5.1 母表字段定义(8 核心 + 3 辅助 = 12 列,固定不再改)

#### A. 8 个核心字段(判断结构)

| 字段 | 定义 | 填写要求 | 最常见误填 |
|---|---|---|---|
| **事物节点** | 模块提供的"稀缺事物" | 写能力 / 容量 / 接口 / 供给,不写板块名 | 把"光模块""PCB""服务器"当最终答案 |
| **约束节点** | 当前决定能否扩、能否赚、能否兑现的主约束 | 只写 1-2 个最硬约束 | 把所有利空都堆上去 |
| **时钟位置** | 处在早段 / 前中段 / 中段 / 中后段 / 后段 | **必须同时写"经营兑现位置"和"市场定价位置"** | 只写景气不写预期位置 |
| **利润池深度** | 最终能留在公司与股东手里的利润深浅 | "很深 / 深 / 中深 / 中 / 中浅 / 浅" | 把收入弹性当利润深度 |
| **供给弹性** | 需求上来时供给多久能补上 | "低 / 中低 / 中 / 中高 / 高" | 把重要性误当成低供给弹性 |
| **财务弹性** | 代表公司整体是高现金/高 FCF/低杠杆还是相反 | "强 / 中强 / 中 / 中弱 / 弱" | 只看 PE,不看现金转换和杠杆 |
| **当前估值状态** | 低估 / 合理 / 拥挤 / 过热 / 重要性高于热度 | **必须写成完整判断句**,不写"贵/便宜" | 单字评级 |
| **适合动作** | 核心跟踪 / 优先深挖 / 选择性跟踪 / 交易观察 / 回避 | **用仓位动作语言,不用研究态度语言** | 写成"看好/不看好" |

#### B. 3 个辅助字段(强制保留,否则母表会退回"静态观点表")

| 字段 | 作用 |
|---|---|
| **代表公司** | 让模块落到股票池 |
| **关键跟踪变量** | 让母表和现实世界保持联动 |
| **失效触发** | 让排序能被及时重排 |

### 5.2 母表填写纪律(5 条,强制)

1. **先填"事物节点",再填其他列** — 后七列都依赖第一列。"光模块"不是事物节点,"高速光连接能力"才是。
2. **"约束节点"只写最硬的 1-2 个** — 写真正会改变排序的约束,不是风险清单。
3. **"时钟位置"必须写两个时钟** — 经营兑现时钟 + 市场定价时钟分开写。
4. **"利润池深度"要和"重要性"分开** — 服务器、ODM、PCB、测试链都可能很重要,但利润池未必深。
5. **"适合动作"写仓位动作,不写研究态度** — 核心跟踪 / 优先深挖 / 选择性跟踪 / 交易观察 / 回调优先看 / 回调先回避。

### 5.3 母表使用手册

**A. 它首先不是股票池,而是"模块排序器"** — 把"真核心 / 扩散层 / 重要但利润浅 / 高 beta 交易层 / 最怕信用收缩"的判断**固定下来**。

**B. 每次更新只检查 3 列**:约束节点 / 时钟位置 / 当前估值状态。其他列通常会跟着变。

**C. 触发整表重排的是结构变量变化,不是股价** — 每份报告必须给出 ≥5 条"失效触发信号"映射表(哪个信号触发哪一列重写)。

**D. 8 问工作法**(打开母表时按这个顺序问):

1. 真正提供的稀缺事物是什么
2. 当前最硬的约束是什么
3. 处在哪个兑现时钟上(经营 vs 定价)
4. 利润最终能不能留在股东手里
5. 供给会不会很快补上
6. 资产负债表能不能穿过错配
7. 市场是低估、合理、拥挤还是过热
8. 现阶段最适合的动作是什么

8 问答完,模块排序基本不跑偏。

### 5.4 母表规模要求

- 行数 = **每个独立"事物节点"一行**(AI 链典型 13-15 行,EV / 航空等 8-12 行)
- 每行 12 列必须全部填写,不允许留白
- 附一张空白模板(供以后板块切换)

---

## 六、六部分收敛结构(v1.0 骨架,v2.0 保留并映射到四层)

这是产业链报告的**叙事骨架**。21 个分析角度分配到 6 个部分,**每个部分对应四层系统图的某一层(或多层)**:

| 部分 | 名字 | 核心任务 | 对应四层 | 对应章节 |
|------|------|---------|--------|---------|
| **A. 地图** | 谁在哪 | 四层结构 + 玩家数 + 市值分档 | **结构层** | Ch 1-3 |
| **B. 尺子** | 怎么判 | 四把尺子 + 分层估值 + 离散度 + SBC 幻觉 | **约束层 + 定价层** | Ch 4-7 |
| **C. 时间** | 什么时候 | 订单瀑布 + 利润归属 + CapEx 分配 | **时钟层** | Ch 8-10 |
| **D. 结构** | 谁会先破 | 信用传导 + 象限模型 + 三种贵 + 拥挤度 | **约束层 + 定价层** | Ch 11-14 |
| **E. 情景** | 会怎样 | 三情景 + 压力测试 + 混读组 | **时钟层 + 定价层** | Ch 15-17 |
| **F. 行动** | 做什么 | 三池 + 回调应对 + 跟踪 + 投资者分型 + 钉子 | **定价层** | Ch 18-22 |

**v2.0 新增章节约束**: Part A 的第一章必须是 **"总定义:这张图不是产业链,是预算-制造-部署-回流闭环网络"**,明确四层系统图;倒数第二章必须是 **"九个钉子 + 12 列母表"**(母表作为可复用底板显式出现)。

---

## 七、21 个分析角度(v1.0 保留,映射到四层)

### Part A: 结构层(3 角度)

#### 角度 1:四层结构 + 信用弱点第四层

把产业链画成三层**物理/商业/经济**结构,再叠加第四层**信用弱点**(横跨前三层)。

| 层级 | 含义 | AI 链示例 | 通用模板 |
|------|------|----------|---------|
| L1 物理瓶颈层 | 决定"够不够"的核心环节 | GPU / HBM / 先进制程 / EUV | 行业核心不可替代环节 |
| L2 扩散层 | 决定"能不能交付"的执行环节 | 光模块 / PCB / 配电 / 服务器 | 围绕核心的关键配套 |
| L3 外围商业化层 | 决定"变成钱"的最终通路 | SaaS / 公用事业 / REIT | 终端收入承接者 |
| L4 信用弱点层 | 横跨前三层,下轮去杠杆时先被重定价 | ORCL / LITE / HPE | 满足 ≥2 条:净债/EBITDA>5x、FCFE 转负、CapEx/Rev>40%、Current Ratio<1、Goodwill/Assets>50%、单一客户>30% |

**强制**: L4 必须单独显式化,不能隐性埋藏在风险章里。

#### 角度 2:每层玩家数量 + 定价权趋势

对每个节点列:玩家数量(1 家垄断 / 2-3 家寡头 / 4-6 家分化 / >6 家充分竞争)+ 定价权趋势(过去 2 年增强 ↑ / 稳定 → / 削弱 ↓)。

#### 角度 3:按市值分档扫描

大盘>$500B(信用强,回调是避险)/ 中大盘$100-500B(估值分化最明显)/ 中盘$10-100B(基本面 vs 估值差异最大)/ 小盘<$10B(质量异类 vs 高流动性风险)。

---

### Part B: 约束层 + 定价层(4 角度)

#### 角度 4:四把尺子(核心判断框架)

任何公司都必须同时通过四把尺子才能进入"核心池":

1. **节点不可替代性**(护城河):3 级——最不可替代 / 高但非垄断 / 易被补
2. **利润留存能力**(定价权):毛利率稳定性 < 5pp 波动;ROIC > WACC + 5pp;SBC/Rev < 10%
3. **资本开支回报可测性**:ROI 是否被 guidance 过 / CapEx 和收入的先后顺序 / 是否分开披露"增长性"与"维持性"
4. **衰退/回调中的信用弹性**:净债/EBITDA<3x、Current Ratio>1.5、FCFE>0

**强制**: 报告必须列出"四把尺子同时通过的名单"(通常只有 5-10 家)。

#### 角度 5:分层估值尺子

| 子链类型 | 不该只看什么 | 应该同时看 |
|---------|-------------|-----------|
| 核心垄断节点 | PE | PE + 毛利稳定性 + FCF + ROIC |
| 周期内存/原料 | Forward PE | 跨周期利润 + EV/EBITDA + 供给曲线 |
| 重资产代工/生产 | PE | PE + EV/EBIT + CapEx intensity + FCF |
| 关键设备 | 收入增速 | PE + ROIC + 订单质量(backlog + book-to-bill) |
| 高客户集中度零部件 | 收入和 PE | EV/EBITDA + 净债 + 客户集中 + ASP 趋势 |
| 组装/集成 | PE | EV/EBITDA + 营运资本 + 净债 + 现金转换 |
| 重并购成长 | PE | PE + backlog + book-to-bill + FCF + 商誉占比 |
| 软件订阅 | PE | FCF + usage(非 seat) + workflow 迁移证据 |

#### 角度 6:估值离散度诚实性测试

对核心公司同时用 4 种方法(DCF / EV/EBITDA / P/FCF / 相对估值),计算离散度 = (最高 - 最低) / 均值。<15% 质量可信 / 15-30% 合理 / 30-50% 警告 / >50% 极高。

#### 角度 7:SBC 稀释量化(Owner PE 幻觉倍数)

对 SBC/Rev > 5% 的公司,Owner PE = 市值 / (GAAP 净利润 - SBC);幻觉倍数 = Owner PE / Non-GAAP PE。>2.0x 时 Non-GAAP 是永久性光学折扣。

---

### Part C: 时钟层(3 角度)

#### 角度 8:订单瀑布 + 时滞

把"预算决定 → 订单 → 收入 → 利润 → 现金"拆成 8 个时点(T+0、T+60d、T+90d、T+180d、T+270d、T+360d、T+540d、T+720d),每个时点标出受益公司。

#### 角度 9:利润最终归属五层

区分"谁拿收入"和"谁拿利润":预算决定层 → 收入承接层 → 毛利承接层 → 现金流承接层 → 股东回报承接层。

#### 角度 10:CapEx / 总需求分配到供应链的映射

把本轮行业总资本开支(或总 TAM)按百分比分配到各子链。市值隐含份额 > 实际份额 2 倍 = 身份溢价型贵。

**v2.0 补充:八个时钟全景表**

必须在 Part C 显式呈现 8 个时钟的全景表:预算 / 设计与资格导入 / 制造扩产 / 交付 / 上电与热管理 / 商业化回流 / 现金兑现 / 市场认知。每个时钟给出:当前位置 + 最先信号 + 典型错配案例。

---

### Part D: 约束层 + 定价层(4 角度)

#### 角度 11:信用传导五层瀑布(带监测点)

```
Layer 1(需求决定者)→ Layer 2(核心硬件/材料)→ Layer 3(扩散层)→ Layer 4(配套与执行)→ Layer 5(二级组件)
DPO / lead time 随层数递减
```

5 个监测点任一触发 → 整链重定价:Top 3 CapEx guidance 下修 >10-15% / utilization 低于经济阈值 / DPO 延长 >70 天 / 扩散层 Current Ratio < 1.0 / 信用弱点层净债/EBITDA 新增 >1x。

#### 角度 12:二维象限模型(周期位置 × 信用强弱)

Q1 前中段 × 信用强 = 核心重点研究 / Q2 前中段 × 信用弱 = 交易型 / Q3 中后段 × 信用弱 = 回避 / Q4 中后段 × 信用强 = 核心但贵(回调再进)。

#### 角度 13:三种"贵"

质量型贵(回调 -25%~-40%)/ 题材型贵(-50%~-80%)/ 身份溢价型贵(温水 -25%~-40%)。

#### 角度 14:拥挤度 + 持有人脆弱性

主题 ETF 持仓占比(>20% = 高拥挤)+ 对冲基金多空净仓位 + 长期价值型 vs 高 beta 资金占比。PE > 100x 或涨幅 > +300% 必须标注。

---

### Part E: 时钟层 + 定价层(3 角度)

#### 角度 15:三种宏观情景 + 概率加权

Bull / Base / Bear 三情景:具体触发条件(不是概念)+ 当前概率 + 每家公司涨跌幅 + 按概率加权的期望收益排序。

#### 角度 16:压力测试七场景

最少 7 个:龙头下游增速跌破阈值 / 终端 CapEx guidance 下修 / 利率突变 / 主题 ETF 单月 -15% / 外生事件 / 关键原料单月 -20% / 货币政策转向。

#### 角度 17:混读组分析(≥5 组)

"节点相近但投资逻辑不同"的公司组合,≥5 组。每组展示:为什么容易被混 / 哪个维度完全不同 / 数据表对比 / 投资含义。

---

### Part F: 定价层(5 角度)

#### 角度 18:三池分配(核心 / 研究 / 交易 / 回避)

核心池 = 四把尺子同时过 + Q1 或 Q4;研究池 = 三把尺子过 + 等触发;交易池 = 高 beta / 高拥挤 / 短期窗口;回避池 = L4 信用弱点 + 基本面弱。每家覆盖公司必须定位,不许留白。

#### 角度 19:回调应对(四层操作)

A. 回调优先重看(越跌越值得)B. 需要确认节奏(等 -35~-45% 再入场)C. 只做反弹交易(1% 仓位 + 严格止损)D. 回调时先回避(便宜 ≠ 安全)。

#### 角度 20:跟踪清单(按频率分层)

每日(10 分钟)/ 每周 / 每月 / 每季度(财报季)/ 每年(结构性变量)。每个指标必须标注数据源 + 触发重排序的具体阈值。

#### 角度 21:九个钉子固化(读者带走什么)

正好 9 个,每个 ≤40 字,必含具体公司名或数字,加起来覆盖 Part A-E 每一部分至少一次。1-3 个最意外 / 4-6 个最可操作 / 7-9 个最系统性。

**v2.0 补充:倒数第二章必须并列呈现"九个钉子 + 12 列母表"**。钉子是叙事压缩,母表是结构压缩,两者互补不可替代。

---

## 七点一、v3.0 硬产出 #1:执行摘要(3 页,5 问固定)

**位置**: 主报告第一章,在"零、阅读指南"之前。不允许后置、不允许压缩成摘要段落。

**5 问固定结构**(缺任一 = 报告不合格):

1. **现在最该看哪三层** — 3 个模块 + 代表公司 + 一句话为什么
2. **现在最不该追哪三层** — 3 个模块 + 代表公司 + 一句话为什么不追
3. **Top 10 真正该跟踪的名字** — 跨三张榜单浓缩,依次列出,必须覆盖主利润池 / 长周期承接 / 关键配套三个位置
4. **三条最关键触发器** — 每条必须是可验证的外部信号(CapEx 指引、利用率、backlog 方向、ASP 同比),不是感受
5. **三个最可能打脸的风险** — 每条必须指向具体模块 + 具体传导链,不是宏观不确定性

**长度硬约束**: 800-1500 字,不允许超过 3 页。如果读者只读这 3 页,也能做出"关注哪三层、避开哪三层、盯什么信号"的可执行判断。

**阅读路径**: 执行摘要末尾必须给出三条阅读路径(只想要决策 / 想理解方法 / 想要复盘工具),分别指向报告不同章节。

---

## 七点二、v3.0 硬产出 #2:三榜决策清单(禁止合并为单一 Top 20)

**位置**: 主报告第十章,替代 v1.0/v2.0 的"Top 20 重点公司清单"。

**三张榜的硬分工**:

| 榜 | 名称 | 回答什么 | 排序依据 | 动作语言 |
|---|---|---|---|---|
| 榜一 | **结构性核心池** | 谁最不可绕开 | **结构质量分**(见七点四) | 核心跟踪 / 回调优先看 |
| 榜二 | **当前研究优先池** | 谁的重要性高于热度 | **当前定价分**(见七点四) | 中期研究 / 优先深挖 |
| 榜三 | **交易回调池** | 谁更适合等价格和节奏 | **财务韧性分倒序** | 交易观察 / 回避 |

**硬纪律**(缺任一 = 三榜失效,退回单一 Top 20):

1. **一家公司只进一张榜**,不允许重复出现(即使同时满足两榜要求,按"更靠前的榜"归属)。
2. **平台层**(`MSFT / GOOGL / META / ORCL` 等)**不套用制造业结构质量分**,独立列在榜一末尾。
3. **三榜合计必须覆盖 ≥25 个名字**(比单一 Top 20 多 5 家以上),多出的是原先被"其他重要名字"掩埋的配套层和财务结构类。
4. **禁止对"主利润池"和"回调交易层"给同一套排名** — 这是 Top 20 单一排名最大的问题。

---

## 七点三、v3.0 硬产出 #3:模块证据卡(每个模块一张,7 字段固定)

**位置**: 主报告"四点六、模块证据卡"整章。每个事物节点对应一张卡,AI 链典型 13-15 张。

**7 字段固定格式**(缺任一字段 = 卡片不合格):

| 字段 | 要求 |
|---|---|
| **3 个支持事实** | 每条含来源链接或数字;不允许空泛判断 |
| **2 个削弱事实** | 必须是能实际推翻结论的反证,不是"宏观风险" |
| **1 张关键图指向** | 明确指向四张核心图之一(见七点六) |
| **1 个失效触发** | 可验证的外部信号,不是"如果不好就不行" |
| **1 个最优表达公司** | 明确写"这个模块里首选 X 而不是 Y",附原因 |
| **1 个最危险的错法** | 指出市场最常犯的具体错误,例如"把 A 当 B 的同类" |

**硬纪律**:

1. **每张卡片都可被独立推翻** — 任一支持事实失效或削弱事实规模扩大到可改变结论时,对应模块在下一次复盘时自动降级。
2. **证据卡的结论必须与三榜 + 三分系统一致** — 证据卡说"最危险错法是当核心资产",对应模块不能出现在榜一。
3. **证据卡是自我证伪机制,不是观点展示** — "削弱事实"章节写不出来的模块 = 还没研究透,必须回头补。

---

## 七点四、v3.0 硬产出 #4:三分系统(三套分数各司其职,禁止相加)

**位置**: 主报告"二十七点五、三分系统"章 + `data/three_score_table_{DATE}.md`。

**三套分数的分工**:

| 分数 | 衡量什么 | 满分 | 驱动哪张榜 |
|---|---|---|---|
| **结构质量分** | 结构关键性 + 利润池厚度 + 供给刚性 + 财务韧性 + 当前定价性价比 | 25 | 榜一排序 |
| **当前定价分** | 重要性/热度剪刀差 + PE vs 利润留存 + 订单可见性 + 拥挤度 + 失效触发清晰度 | 25 | 榜二排序 |
| **财务韧性分** | 净债/EBITDA + Current Ratio + FCFE 方向 + 客户集中度 + Goodwill/Assets | 5 | 榜三排序(倒序) |

**硬纪律**(缺任一 = 三分系统失效):

1. **三套分数禁止相加**。相加会让光模块因为结构关键性高被误排进核心池,或让 `ETN` 因为当前定价分高被误当作"最优持仓"。
2. **每套分数只决定一件事** —
   - 问"该不该放核心"→ 结构质量分
   - 问"现在该不该优先研究"→ 当前定价分
   - 问"回调里先回避谁"→ 财务韧性分
3. **结构质量分 ≥20 = 榜一候选**;**当前定价分 ≥16 = 榜二候选**;**财务韧性分 ≤10 = 榜三"回调先回避"子档**。
4. **每个分数必须给出五维打分细节**,而不是直接给总分。五维可审计,总分不可单独引用。

---

## 七点五、v3.0 硬产出 #5:变化日志(每次更新只写四件事)

**位置**: `reports/{CHAIN_NAME}/data/changelog_{DATE}.md` + 主报告"三十二点五"章。

**四字段固定结构**:

| 字段 | 定义 |
|---|---|
| **新增证据** | 本次新增的数据点 / 公开材料 / 公司披露,附来源链接 |
| **删除证据** | 上一次使用但此次已失效或被更强证据替代的条目 |
| **排序变化** | 三张榜单中任一名字的顺序 / 进出变化,附对应原因 |
| **失效条目** | 上一次列出的失效触发中,本次已经触发或已被证伪的条目 |

**硬纪律**:

1. **首次发布允许 "新增证据" 有内容,其他三项为空** — 但模板必须四项齐全。
2. **每次复盘不允许重写观点,只能通过这四个字段更新** — 这强制让研究变成累积式资产,而不是每次从头再写。
3. **变化日志必须附"下一次复盘的硬触发"清单**(5-7 条)— 任一触发即启动下一轮更新,不等既定季度。

---

## 七点六、v3.0 硬产出 #6:四张核心图(每张必须指向代表模块)

**位置**: 主报告附录 + 证据卡"关键图"字段显式引用。

**四张图的硬分工**:

| 图名 | 回答什么 | 坐标 | 必须出现的模块 |
|---|---|---|---|
| **预算—订单—收入—现金瀑布图** | 传导时滞 | 时间轴 × 模块 | 平台 → GPU/HBM → 封装/设备 → 网络/光模块 → 服务器 → 配电/热管理 → 软件回流 |
| **利润池 × 供给刚性二维图** | 真正的厚利润池在哪 | 利润池深度(X)× 供给弹性(Y,倒轴) | 右上角必含 GPU / HBM / Foundry / 关键设备 |
| **模块周期钟** | 每个模块在哪一段 | 经营兑现时钟 × 市场定价时钟 | 12 点位置必含最核心模块,**两个时钟分开画,不是合成单钟** |
| **拥挤度 × 财务韧性四象限图** | 回调里谁先脆 | 拥挤度(X)× 财务韧性(Y) | 右下象限必含 `LITE / HPE`,左上必含 `ETN / HUBB / POWL` |

**硬纪律**:

1. **证据卡的"关键图"字段必须指向这四张图之一**,不允许指向一张不存在的图。
2. **四张图合计 ≥4 张(不是"至少一张")** — 缺任一张 = 主报告可视化不合格。
3. **图可以用文字结构模拟**(模块 + 坐标 + 代表公司),但坐标不能缺。

---

## 七点七、v3.0 硬产出 #7:母表可审计字段(12 + 4 = 16 列)

**位置**: `data/module_master_table_{DATE}.md`,正文保留压缩版,完整版在数据附录。

**原 12 列基础上新增 4 列可审计字段**:

| 字段 | 说明 |
|---|---|
| **数据口径** | 例如"trailing PE, GAAP 基础"或"debt/equity 账面口径(非经营净杠杆)" |
| **最新更新时间** | 精确到日 |
| **来源链接** | 公司 10-K / 年报 / factsheet / 投资者材料直链 |
| **判断置信度** | 高 / 中 / 低;**置信度 <中 的单元格不得单独驱动排序变化,必须另找证据** |

**硬纪律**:

1. **每一格都独立可审计** — 任一格可被替换,不必从散文反查结论。
2. **正文只引用压缩版**(标注 `[K-##]` 编号),完整 16 列表放附录。
3. **来源链接必须是公开、可直接访问的 URL** — 不允许写"内部数据""市场共识""业内人士"。

---

## 八、执行流程(10 步,v2.0 新增 Step 4.5 + Step 8.5)

### Step 1: 链条范围确认

CHAIN_NAME + 覆盖公司数(建议 20-60 家) + 时间锚点(精确到日)。

### Step 2: 读取已有独立公司报告

```bash
ls reports/ | grep -iE "{CHAIN_KEYWORDS}"
```
≥5 家有 Tier 3 深度报告时,优先用其公允价值作为评级锚。

### Step 3: 拉取 FMP 最新季度数据

12 项必须齐全:PE / EV/EBITDA / EV/Sales / ROE / ROIC / 净债/EBITDA / Current Ratio / Quick Ratio / FCFE(季度)/ CapEx/Revenue / SBC/Revenue / Goodwill/Total Assets。

**禁止使用年化代理**。

### Step 4: 构建 chain_skeleton.yaml

```yaml
chain_name: {CHAIN_NAME}
date: {YYYY-MM-DD}
four_layer_system:
  structure_layer:
    budget_source: [...]
    physical_backbone: [...]
    deployment_diffusion: [...]
    reflow_commercialization: [...]
  constraint_layer:
    budget_constraint: [...]
    physical_supply_constraint: [...]
    deployment_constraint: [...]
    profit_retention_constraint: [...]
    regional_role_constraint: [...]
  clock_layer:
    budget_clock: {...}
    design_qualification_clock: {...}
    manufacturing_clock: {...}
    delivery_clock: {...}
    power_thermal_clock: {...}
    commercialization_clock: {...}
    cash_realization_clock: {...}
    market_perception_clock: {...}
  pricing_layer:
    structural_premium: [...]
    thematic_premium: [...]
    pricing_quadrants: {A: [...], B: [...], C: [...], D: [...]}

four_rulers:
  pass_all_4: [...]

quadrants:
  Q1_early_strong: [...]
  Q2_early_weak: [...]
  Q3_late_weak: [...]
  Q4_late_strong: [...]

three_types_of_expensive: {quality: [...], theme: [...], identity_premium: [...]}
three_scenarios: {bull: {...}, base: {...}, bear: {...}}
pools: {core: [...], research: [...], trading: [...], avoid: [...]}
nine_nails: []
```

### Step 4.5(v2.0 新增): 构建 12 列母表

产出 `reports/{CHAIN_NAME}/data/module_master_table_{DATE}.md`,含:

1. **12 列定义表**(8 核心 + 3 辅助 + 每列"最常见误填")
2. **5 条填写纪律**
3. **主母表**(每个事物节点一行,12 列全填)
4. **失效触发映射表**(≥5 条信号 → 哪一列重写)
5. **8 问工作法**
6. **空白模板**(供以后板块切换)

母表必须先于 Step 5 开始填——**母表填不完,主报告禁止写**。这是防止 Step 5 退回散文式观点表的硬控制。

### Step 5: 按六部分收敛结构撰写章节

严格按 Part A→B→C→D→E→F,不允许重排。Part A 第一章必须是"总定义:四层系统图"。

### Step 6: 压力测试 + 混读组

三情景每家公司涨跌幅具体数字 ✓ / 7 个压力测试齐全 ✓ / ≥5 组混读组 ✓。

### Step 7: 跟踪清单 + 投资者分型

至少 4 种投资者:长线价值 / 成长 GARP / 主题交易 / 风险厌恶。

### Step 8: 九个钉子写作

倒数第二章。≤40 字 / 必含数字或公司名 / ≥3 个反直觉。

### Step 8.5(v2.0 新增): 四个核心修正 + 8 问回头验证

最终报告完成后,用四个核心修正 + 8 问工作法**逐一反查**:

- 是否写成了模块图而不是事物图?
- 是否只谈受益不谈作用?
- 是否只有一个静态时钟没有拆成多钟?
- 是否只评"重要"没评"值得拿 / 只适合交易"?
- 8 问每一问是否都能从母表某一列直接读出答案?

任一反查失败 → 回到对应章节重写。

### Step 9: 无痕化检查

```bash
grep -cE "v[0-9]+|原报告|修订说明|重写|Phase|Agent|staging|skill" {REPORT}
# 期望: 0
grep -cE "回流|校准|深度研报|本次|Tier [0-9]" {REPORT}
# 期望: 0 或仅限正当用法
```

### Step 10: 交叉一致性检查(v3.0 扩展)

下列四道一致性检查全部通过才能提交:

1. **母表 ↔ 三榜**:母表"适合动作"列必须与三榜分配(七点二)一一对应。不允许某模块母表写"核心跟踪"却不进榜一。
2. **三分系统 ↔ 三榜排序**:榜一按结构质量分降序;榜二按当前定价分降序;榜三按财务韧性分升序。不允许手工覆盖分数。
3. **证据卡 ↔ 四张核心图**:每张证据卡的"关键图"字段必须指向七点六的四张图之一。
4. **变化日志 ↔ 失效触发**:变化日志的"下一次复盘硬触发"必须覆盖所有证据卡的失效触发。

### Step 11(v3.0 新增): 7 项硬产出齐全性检查

提交前脚本化自检:

```bash
# 1. 执行摘要(5 问结构)
grep -c "现在最该看哪三层\|现在最不该追的三层\|Top 10 真正该\|三条最关键触发器\|三个最可能打脸的风险" {REPORT}
# 期望: 5

# 2. 三榜结构
grep -c "结构性核心池\|当前研究优先池\|交易回调池" {REPORT}
# 期望: ≥3

# 3. 证据卡数量(每个模块一张)
grep -cE "^### 4\.6\.[0-9]+" {REPORT}
# 期望: ≥10(AI 链典型 13-15)

# 4. 三分系统
grep -c "结构质量分\|当前定价分\|财务韧性分" {REPORT}
# 期望: ≥3

# 5. 变化日志
grep -c "新增证据\|删除证据\|排序变化\|失效条目" {REPORT}
# 期望: ≥4

# 6. 四张核心图指向
grep -c "预算—订单—收入—现金瀑布图\|利润池 × 供给刚性二维图\|模块周期钟\|拥挤度 × 财务韧性四象限图" {REPORT}
# 期望: ≥4(每张图至少被引用 1 次)

# 7. 16 列母表(12 + 4 审计字段)
grep -c "数据口径\|最新更新时间\|来源链接\|判断置信度" data/module_master_table_*.md
# 期望: ≥4
```

任一项不通过 → 回到对应章节补齐。

---

## 九、产出规格(v3.0 扩展)

### 9.1 主文件:`reports/{CHAIN_NAME}/Chain_Framework_{YYYY-MM-DD}.md`

| 项 | 要求 |
|---|---|
| 字符数 | **140-200K**(v3.0 因新增执行摘要 + 证据卡 + 三榜 + 三分 + 变化日志,下限从 70K 抬至 140K) |
| 章节数 | 30+ 章(6 部分 + v3.0 新增 7 项硬产出章节)+ 目录 + 结语 |
| **v3.0 必含章** | 执行摘要(5 问)/ 总定义(四层)/ 模块证据卡(每模块一张)/ 三榜决策清单 / 三分系统 / 变化日志 / 九个钉子 + 12 列母表 |
| 附录数 | 10-14 个 |
| 数据表 | ≥30 张 |
| **四张核心图** | **必须齐全**(文字 + 坐标模拟允许) |
| 覆盖公司 | 25-60 家(v3.0 下限 25,因三榜合计覆盖更广) |

### 9.2 骨架文件:`reports/{CHAIN_NAME}/data/chain_skeleton.yaml`

字段见 Step 4,v2.0 新增 `four_layer_system` 字段集,v3.0 新增 `three_lists`、`three_scores`、`evidence_cards` 字段。

### 9.3 母表文件:`reports/{CHAIN_NAME}/data/module_master_table_{DATE}.md`

**v3.0 = 12 列核心 + 4 列可审计 = 16 列活数据库**。字段详见七点七。**缺此文件不得提交主报告**。

### 9.4(v3.0 新增): 三分系统评分表 `data/three_score_table_{DATE}.md`

- 结构质量分五维 + 总分
- 当前定价分五维 + 总分
- 财务韧性分五维 + 总分
- 每家公司对应的榜单归属

### 9.5(v3.0 新增): 变化日志 `data/changelog_{DATE}.md`

四字段固定结构(七点五),附下一次复盘的硬触发 5-7 条。

---

## 十、泛化指引(迁移到其他行业)

| 行业 | L1 物理瓶颈 | L2 扩散 | L3 商业化 | L4 信用弱点候选 |
|------|------------|---------|----------|-----------------|
| AI | GPU / HBM / 先进制程 | 网络 / 光模块 / 配电 | SaaS / DC REIT | 高 CapEx 云厂 / 高杠杆光模块 |
| EV | 电池材料 / 碳化硅 / 电驱 | 充电桩 / 电池管理 / 轻量化 | 整车 OEM | 高负债新势力 / 商誉重的并购 OEM |
| 肥胖药 (GLP-1) | API 原料药 / 肽合成 | Pen 装置 / 包装 / 冷链 | CRO / 支付 / 保险 | 单一产品依赖 + 专利悬崖 |
| 数据中心电力 | 核电 / 燃气轮机 | 变压器 / UPS / 配电柜 | IPP / 电网 / REIT | 重补贴 / 重并购 / 长周期 PPA |
| 太空 | 火箭 / 卫星载荷 / 芯片抗辐射 | 地面站 / 发射场 / 保险 | 卫星电信 / 遥感服务 | 未盈利 SPAC / 政府合同依赖 |
| 半导体设备 | EUV / 缺陷检测 | 刻蚀沉积 / ALD / CMP | OSAT / 测试服务 | 高中国敞口 / 高客户集中 |
| 生物药 CDMO | 细胞培养 / 抗体纯化 | 填装 / 冷链 / QC | 临床阶段药企 / 大药企 | 单客户 >40% |

**v2.0 迁移操作**: 换板块时只换母表数据,不换 12 列结构。先填母表 → 再写四层叙事 → 再补 21 角度深度。**母表是泛化的锚**。

**判断 L4 的通用标准(不分行业)**: 同时满足 ≥2 条:净债/EBITDA > 5x / FCFE 转负 / CapEx/Revenue > 40% / Current Ratio < 1.0 / Goodwill/Total Assets > 50% / 单一客户 > 30%。

---

## 十一、反向约束(NEVER,v1.0 10 条 + v2.0 新增 4 条 + v3.0 新增 7 条)

1. **NEVER 只列玩家不问"谁拿利润"** — 五层利润归属必跑
2. **NEVER 把节点相近的公司写成"差不多"** — 混读组 ≥5 组
3. **NEVER 用年化代理替代季度口径**
4. **NEVER 跳过 L4 信用弱点层** — 必须独立显式化
5. **NEVER 用单一估值方法** — 核心公司跑 4 种离散度
6. **NEVER 只用 Non-GAAP PE 讨论 SBC/Rev > 5% 的公司**
7. **NEVER 把"宏观不确定性"当风险主段** — 风险按子链写
8. **NEVER 给情景只打"正/中/负"** — 必须具体触发 + 涨跌幅
9. **NEVER 写 >9 或 <9 个钉子** — 正好 9 个,每个 ≤40 字
10. **NEVER 在最终报告暴露过程** — 无痕化硬门控

**v2.0 新增:**

11. **NEVER 跳过 12 列母表** — 母表缺任一列或任一行 = 报告不合格。这是 v2.0 防止退回"散文式观点表"的硬控制。
12. **NEVER 把"重要"写成"最适合买"** — 修正 4 的反向约束。母表"适合动作"列必须使用仓位语言(核心跟踪/研究/交易/回避),不用研究态度语言(看好/不看好)。
13. **NEVER 只写一个时钟** — 修正 3 的反向约束。每个模块的"时钟位置"列必须同时写经营兑现时钟和市场定价时钟。
14. **NEVER 把"事物节点"写成板块名** — 修正 1 的反向约束。"光模块""服务器""PCB"不是事物节点,"高速光连接能力""AI 系统交付与集成能力""板级高速互连能力"才是。

**v3.0 新增(固化 7 项硬产出):**

15. **NEVER 把 Top 20 做成单一总榜** — 必须拆成结构性核心池 / 当前研究优先池 / 交易回调池三张榜。一家公司只进一张榜。
16. **NEVER 省略 3 页执行摘要** — 5 问结构必须齐全(该看哪三层 / 不该追哪三层 / Top 10 名字 / 三条触发器 / 三个打脸风险),长度 800-1500 字。
17. **NEVER 省略模块证据卡** — 每个事物节点一张卡,7 字段固定(3 支持 + 2 削弱 + 1 关键图 + 1 失效触发 + 1 最优表达 + 1 最危险错法)。"削弱事实"字段为空 = 研究不透,必须回补。
18. **NEVER 把三分系统相加成总分** — 结构质量分 / 当前定价分 / 财务韧性分三套独立,各驱动一张榜。相加会制造错误排序。
19. **NEVER 省略变化日志** — 每次更新只写四件事(新增 / 删除 / 排序变化 / 失效条目),首次发布时其他三项可为空但字段必须齐全。
20. **NEVER 省略四张核心图** — 预算—订单—收入—现金瀑布 / 利润池×供给刚性 / 模块周期钟(双时钟分开) / 拥挤度×财务韧性四象限,每张必须指向具体模块。
21. **NEVER 让母表只有 12 列** — v3.0 必须扩展为 16 列(12 核心 + 数据口径 / 最新更新时间 / 来源链接 / 判断置信度)。来源链接必须是公开可访问 URL,不允许"内部数据"。

---

## 十二、数值预算

| 指标 | 阈值 |
|------|------|
| 单次执行总 token 消耗 | ≤200K(v3.0 上调,因新增执行摘要 + 证据卡 + 三榜 + 三分 + 变化日志) |
| 每家公司 FMP 数据拉取 | 1 次 |
| 章节最长 | ≤6000 字 |
| 章节最短 | ≥1500 字 |
| 附录最多 | ≤14 个 |
| 九个钉子 | 正好 9 个,每个 ≤40 字 |
| 混读组 | ≥5 组 |
| 压力测试场景 | ≥7 个 |
| **v2.0 母表行数** | **每个独立事物节点一行,AI 链典型 13-15 行** |
| **v3.0 母表列数** | **必须正好 16 列(12 + 4 审计),不增不减** |
| **v2.0 失效触发信号数** | **≥5 条,每条映射到某一列** |
| **v3.0 执行摘要字数** | **800-1500 字,≤3 页** |
| **v3.0 证据卡数** | **每个事物节点一张,AI 链典型 13-15 张** |
| **v3.0 三榜覆盖公司数** | **合计 ≥25 家,每家只进一张榜** |
| **v3.0 四张核心图** | **齐全,每张至少被证据卡引用 1 次** |
| **v3.0 变化日志触发器数** | **≥5 条可验证外部信号** |

---

## 十三、与其他 skill 的关系

| 关系 | skill | 用法 |
|------|------|------|
| **前置** | `moat-evaluator` | 单家已跑过的结果引用到"四把尺子/尺子一" |
| **前置** | `valuation-builder` | 单家估值结果进入"角度 6 离散度测试" |
| **前置** | `expectation-gap` | 单家预期差判断进入"角度 13 三种贵" |
| **平行** | `competitive-benchmarking` | 单子链玩家对标用它,整链结构用本 skill |
| **下游** | `harness-controller` | 本 skill 产出是多家 Tier 3 输入之一 |
| **下游** | `content-engine` | 九个钉子 + 12 列母表是传播版核心素材 |

---

## 十四、v1.0 → v2.0 变更清单

| 维度 | v1.0 | v2.0 升级 |
|------|------|---------|
| **顶层架构** | 三层结构 + L4 信用弱点 | **四层系统图**(结构 / 约束 / 时钟 / 定价)+ L4 保留在结构层内 |
| **核心修正指针** | 无 | **四个核心修正**(模块→事物 / 受益→作用 / 静态→多钟 / 重要→值得拿) |
| **可复用底板** | 无 | **12 列母表 + 5 条纪律 + 8 问工作法 + 空白模板** |
| **时钟数量** | 隐含在订单瀑布中 | **八个时钟显式化** |
| **硬产出文件** | 主报告 + chain_skeleton.yaml | 主报告 + chain_skeleton.yaml + **module_master_table.md** |
| **执行步骤** | 9 步 | **10 步**(新增 Step 4.5 母表 + Step 8.5 反查) |
| **NEVER 条数** | 10 | **14**(新增 母表缺失 / 重要≠买 / 单时钟 / 板块名当节点) |
| **Part A 首章** | "链条覆盖范围" | **"总定义:四层系统图"** |
| **倒数第二章** | "九个钉子" | **"九个钉子 + 12 列母表"** |
| **后续扩展** | 无明确路径 | **"模块打分版"**(每列 1-5 分量化规则) |

---

## 十四点五、v2.0 → v3.0 变更清单(固化顶级报告交付标准)

| 维度 | v2.0 | v3.0 升级 |
|------|------|---------|
| **硬产出数量** | 3 项(主报告 / skeleton / 母表) | **7 项**(+ 执行摘要 + 证据卡 + 三分评分表 + 变化日志) |
| **Top 20 呈现** | 单一总榜 | **三张榜**(结构性核心池 / 当前研究优先池 / 交易回调池),一家只进一张 |
| **执行摘要** | 无 | **3 页 5 问固定结构**(看哪三层 / 避哪三层 / Top 10 / 三触发器 / 三打脸风险) |
| **模块证据卡** | 无 | **每模块一张**,7 字段(3 支持 / 2 削弱 / 1 关键图 / 1 失效触发 / 1 最优表达 / 1 最危险错法) |
| **评分系统** | 1 套 25 分(轻量打分版) | **3 套分数各司其职**(结构质量分 / 当前定价分 / 财务韧性分),**禁止相加** |
| **变化日志** | 无 | **4 字段固定**(新增 / 删除 / 排序变化 / 失效条目)+ 下一次复盘硬触发 |
| **核心可视化** | 无明确要求 | **四张核心图固化**(预算瀑布 / 利润池×供给刚性 / 模块周期钟 / 拥挤度×财务韧性) |
| **母表列数** | 12 列 | **16 列**(12 核心 + 4 可审计:数据口径 / 更新时间 / 来源链接 / 判断置信度) |
| **字符下限** | 70K | **140K**(新增交付物撑起字数下限) |
| **NEVER 条数** | 14 | **21**(+ 7 条 v3.0 新增:禁单一 Top 20 / 禁省执行摘要 / 禁省证据卡 / 禁相加 / 禁省变化日志 / 禁省四图 / 禁 12 列母表) |
| **执行步骤** | 10 步 | **11 步**(新增 Step 11:7 项硬产出齐全性检查) |
| **验证案例** | AI 产业地图 v6(2026-04-20) | **AI 产业地图 final(2026-04-20,170K,7 项齐全)** |

---

## 十五、v3.0 已知限制 + 下一步

### 已知限制

- 证据卡的"关键图"字段依赖四张核心图齐全,小链条可能需要降到 2-3 张图,需在卡片中说明
- 三分系统的具体分值需要按行业校准,AI 链上验证的阈值(结构质量分 ≥20 = 榜一候选)可能需要针对电力 / 航空 / 生物等不同行业微调
- 变化日志的价值需要至少 2 次版本迭代才能显现,首次发布时只有"新增证据"一列有内容

### 下一步(v3.1 方向)

1. **四张核心图的可视化工具链**:把文字坐标模拟升级为可直接渲染的 Mermaid / Plotly 模板。
2. **三分系统跨行业校准**:在 EV / 数据中心电力 / 生物 CDMO 上复跑,收敛各行业阈值。
3. **变化日志自动化对比**:比较两次版本的三榜排序差异,自动生成排序变化条目。
4. **证据卡置信度门控**:低置信度证据自动降权,不允许单独驱动排序变化。

---

**一句话定位**: v3.0 把产业链研究从"散文式观点表"(v1.0)→"四层系统 × 12 列母表"(v2.0)→ **"执行摘要 + 三榜 + 证据卡 + 三分 + 变化日志"的顶级研究稿交付标准**(v3.0)。每次运行默认产出 7 项硬交付,从一次性写作转为累积式研究资产——下次复盘只更新关键证据、触发排序变化、写清失效条目,而不是从头再写。
