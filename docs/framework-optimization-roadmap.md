# 分析框架优化路线图

> 版本：v5.0 → v5.1（实战化）
> 核心原则：停止回测过拟合，转向实战验证和系统化

---

## 一、当前框架状态评估

### 已完成

| 项目 | 状态 | 效果 |
|------|------|------|
| v4.0基础框架 | ✅ | 逻辑完整但准确率低 |
| v5.0改进（背离信号等） | ✅ | 准确率提升到93% |
| 跨标的验证 | ✅ | LRCX/AMAT/ASML都有效 |

### 未完成但重要

| 项目 | 重要性 | 状态 |
|------|--------|------|
| 指标量化定义 | 🔴 高 | ❌ 仍有主观判断 |
| 实时数据系统 | 🔴 高 | ❌ 手动获取 |
| 仓位管理规则 | 🔴 高 | ⚠️ 粗略 |
| 样本外验证 | 🔴 高 | ❌ 未开始 |
| 情绪指标量化 | 🟡 中 | ❌ 定性描述 |
| 跨周期验证 | 🟡 中 | ❌ 未做 |

---

## 二、优化方向排序

### 优先级1：停止过拟合，锁定参数

```
立即行动：

1. 固定v5.0所有参数，不再调整
2. 记录当前参数到配置文件
3. 未来任何调整都需要新版本号（v5.1, v5.2...）
4. 每个版本的修改必须有明确的"样本外"理由
```

### 优先级2：指标量化定义

**问题**：当前很多判断是主观的（"极度悲观"、"涨幅收窄"）

**解决方案**：

```python
# 量化定义示例

class FrameworkV5:
    # 周期阶段判断（量化）
    def get_cycle_stage(self, dram_yoy: float, pe_percentile: float) -> str:
        if dram_yoy < -20:
            return "衰退期"
        elif dram_yoy < 0:
            return "复苏期"
        elif dram_yoy < 30 and pe_percentile < 75:
            return "扩张期"
        else:
            return "过热期"

    # 边际变化（量化）
    def calc_marginal_change(self, current_mom: float, prev_mom: float) -> float:
        """
        边际变化 = 本月MoM - 上月MoM
        正值 = 改善（加速上涨或减速下跌）
        负值 = 恶化（减速上涨或加速下跌）
        """
        return current_mom - prev_mom

    # 背离信号（量化）
    def detect_divergence(self, valuation_score: float, leading_score: float) -> tuple:
        """
        返回：(信号类型, 调整分数)
        """
        if valuation_score >= 7 and leading_score <= 3:
            return ("底部背离", +20)
        elif valuation_score <= 3 and leading_score >= 8:
            return ("顶部背离", -20)
        elif valuation_score <= 4 and leading_score >= 7:
            return ("轻度顶部", -10)
        else:
            return ("无背离", 0)

    # 情绪极端（量化）
    def calc_sentiment_score(self,
                             analyst_buy_pct: float,  # 分析师买入评级占比
                             price_vs_52w_high: float,  # 相对52周高点
                             put_call_ratio: float) -> float:
        """
        情绪得分：-10（极度悲观）到 +10（极度乐观）
        """
        score = 0

        # 分析师情绪
        if analyst_buy_pct > 90:
            score += 4  # 一致看多 = 危险
        elif analyst_buy_pct < 50:
            score -= 3  # 分歧或看空 = 可能是机会

        # 价格位置
        if price_vs_52w_high > 0.95:
            score += 3  # 接近高点
        elif price_vs_52w_high < 0.70:
            score -= 3  # 大幅回调

        # 期权市场
        if put_call_ratio > 1.2:
            score -= 2  # 看跌情绪重
        elif put_call_ratio < 0.7:
            score += 2  # 看涨情绪重

        return score
```

### 优先级3：实时数据系统

**问题**：每次分析都需要手动获取数据

**解决方案**：

```
数据自动化架构：

┌─────────────────────────────────────────────────────────────┐
│                     数据层                                   │
├─────────────────────────────────────────────────────────────┤
│  定时任务（每周/每月）                                        │
│  ├── FMP API → 财务数据、估值、内部人交易                     │
│  ├── TrendForce → DRAM现货价（需要订阅或爬虫）                │
│  ├── SEMI → 设备市场数据                                     │
│  └── Yahoo Finance → 分析师评级、期权数据                     │
└─────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────┐
│                     计算层                                   │
├─────────────────────────────────────────────────────────────┤
│  每周自动计算：                                               │
│  ├── 边际变化（MoM的变化）                                    │
│  ├── 背离检测                                                │
│  ├── 周期阶段判断                                            │
│  └── 综合评分                                                │
└─────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────┐
│                     信号层                                   │
├─────────────────────────────────────────────────────────────┤
│  触发条件：                                                   │
│  ├── 评分变化 > 10分 → 发送提醒                              │
│  ├── 背离信号触发 → 重点关注                                  │
│  ├── 边际变化转向 → 预警                                      │
│  └── 季度定期报告                                            │
└─────────────────────────────────────────────────────────────┘
```

### 优先级4：仓位管理规则

**问题**：当前只有粗略的仓位建议（30-40%）

**解决方案**：

```
仓位管理公式：

基础仓位 = f(评分)
  评分 > 80 → 基础仓位 60%
  评分 70-80 → 基础仓位 45%
  评分 55-70 → 基础仓位 30%
  评分 40-55 → 基础仓位 15%
  评分 < 40 → 基础仓位 0%

调整因子：
  Beta调整 = 基础仓位 × (1.5 / 实际Beta)
  集中度调整 = 单一标的不超过总仓位25%
  行业调整 = 同行业总仓位不超过40%

止损规则：
  硬止损 = 买入价 × 0.85（跌15%强制止损）
  软止损 = 评分跌破40分开始减仓

加仓规则：
  评分上升10分 → 可加仓10%
  评分稳定在70+且回调10% → 可加仓
```

### 优先级5：建立投资日志系统

**问题**：没有记录每次决策的理由和结果

**解决方案**：

```
投资日志模板：

日期：YYYY-MM-DD
标的：LRCX
操作：买入/卖出/持有
─────────────────────────────
当时评分：XX分
周期阶段：XX期
背离信号：有/无
边际变化：改善/恶化/持平
─────────────────────────────
决策理由：
（记录为什么做出这个决策）
─────────────────────────────
3个月回顾：
实际涨跌：XX%
评分变化：XX → XX
决策评估：正确/错误/待定
经验教训：
（记录学到了什么）
```

---

## 三、不建议做的优化

### 1. 继续回测更多历史周期

**原因**：
- 已经有过拟合风险
- 2018-2022周期的市场环境（贸易战、COVID）与当前不同
- 可能导致框架越来越复杂

**替代方案**：
- 锁定v5.0，用未来数据验证
- 如果未来表现不好，再回头分析原因

### 2. 添加更多复杂指标

**原因**：
- 复杂度与过拟合正相关
- 难以获取的数据实用性差
- 简单规则往往更鲁棒

**原则**：
- 如果新指标不能提高10%以上准确率，不添加
- 优先使用易获取的公开数据

### 3. 针对单一股票特殊调参

**原因**：
- 会导致框架失去普适性
- 本质上是过拟合

**例外**：
- ASML的EUV特殊性可以作为"行业调整因子"，但要应用于所有EUV相关标的

---

## 四、推荐的优化顺序

```
阶段1：固化和量化（1-2周）
├── 锁定v5.0参数
├── 编写量化定义文档
├── 创建评分模板/工具
└── 输出：framework-v5.0-final.md

阶段2：数据自动化（2-4周）
├── 建立FMP数据自动获取
├── 建立DRAM价格追踪（手动或半自动）
├── 创建周度/月度报告模板
└── 输出：自动化脚本 + 数据仪表板

阶段3：实战验证（持续）
├── 每月用v5.0评估LRCX/AMAT/ASML
├── 记录预测vs实际
├── 积累样本外数据
└── 输出：月度投资日志

阶段4：迭代优化（6个月后）
├── 回顾6个月实战表现
├── 识别系统性偏差
├── 基于样本外数据调整
└── 输出：framework-v5.1.md（如需要）
```

---

## 五、关于跨周期测试的最终建议

### 如果一定要做

```
限制条件：

1. 只用于"验证"，不用于"调参"
   - 如果2018-2022回测失败，记录原因但不改v5.0
   - 只有样本外数据才能触发参数修改

2. 设定"失败阈值"
   - 如果跨周期准确率 < 60%，说明框架过拟合当前周期
   - 但即使如此，也不修改v5.0，而是等待未来验证

3. 记录但不行动
   - 跨周期发现的问题记录下来
   - 作为未来v5.1的参考
   - 但v5.0保持不变
```

### 我的真实建议

```
不做跨周期测试，原因：

1. 边际收益递减
   - 已经有93%准确率
   - 再测可能只是增加"虚假信心"

2. 时间投入不值得
   - 获取2018-2022数据需要工作量
   - 不如投入到数据自动化

3. 真正的检验在未来
   - 2025-2026年的表现才是真正的样本外测试
   - 与其回看过去，不如准备好监控未来
```

---

## 六、立即可执行的下一步

### 选项A：创建v5.0最终版文档

```
输出：
- framework-v5.0-final.md（完整参数定义）
- scoring-template.md（评分模板）
- investment-journal-template.md（投资日志模板）
```

### 选项B：建立数据自动化脚本

```
输出：
- data-fetcher.py（FMP数据获取）
- weekly-report-generator.py（周报生成）
- alert-system.py（信号提醒）
```

### 选项C：开始实战记录

```
输出：
- 2026年1月LRCX/AMAT/ASML评估报告
- 设定3个月/6个月检查点
- 开始积累样本外数据
```

---

*优化路线图版本：1.0*
*创建日期：2026年1月22日*
*核心原则：宁可简单有效，不要复杂过拟合*
